apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitlab
  namespace: gitlab
spec:
  replicas: 1
  # Default is 600s; GitLab first boot + readiness probe often need much longer.
  progressDeadlineSeconds: 3000
  selector:
    matchLabels:
      app: gitlab
  template:
    metadata:
      labels:
        app: gitlab
    spec:
      volumes:
        - name: postgres-password
          secret:
            secretName: app-secret
            defaultMode: 0440
      containers:
        - name: gitlab
          # Pinned tag keeps bootstrap behavior predictable on constrained dev VMs.
          image: gitlab/gitlab-ce:17.11.0-ce.0
          ports:
            # Must match the port in external_url; Omnibus nginx listens there, not 80.
            - containerPort: 8000
              name: http
          volumeMounts:
            - name: postgres-password
              mountPath: /run/secrets/gitlab
              readOnly: true
          env:
            - name: GITLAB_OMNIBUS_CONFIG
              value: |
                external_url 'http://gitlab.local:8000'
                postgresql['enable'] = false
                redis['enable'] = false
                prometheus_monitoring['enable'] = false
                alertmanager['enable'] = false
                registry['enable'] = false
                gitlab_rails['registry_enabled'] = false
                gitlab_rails['db_adapter'] = 'postgresql'
                gitlab_rails['db_encoding'] = 'unicode'
                gitlab_rails['db_host'] = 'postgres'
                gitlab_rails['db_port'] = 5432
                gitlab_rails['db_username'] = 'gitlab'
                gitlab_rails['db_password'] = File.read('/run/secrets/gitlab/POSTGRES_PASSWORD').strip
                gitlab_rails['db_database'] = 'gitlab'
                gitlab_rails['redis_host'] = 'redis'
                gitlab_rails['redis_port'] = 6379
                puma['worker_processes'] = 1
                sidekiq['max_concurrency'] = 5
          startupProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 18
          resources:
            requests:
              cpu: "1200m"
              memory: 2Gi
            limits:
              cpu: "2"
              memory: 3Gi
